# porownanie-podstawowek-mazowsze

## Start projektu (uv)

1. Zainstaluj `uv` (jednorazowo, globalnie).
2. W katalogu projektu uruchom:

```bash
uv venv
uv sync
uv run jupyter lab
```

`uv` utworzy lokalne srodowisko w `.venv`, wiec biblioteki nie koliduja z innymi projektami.
